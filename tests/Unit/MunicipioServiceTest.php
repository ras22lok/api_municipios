<?php

    // use App\Services\MunicipioService;
    // use App\Services\Providers\Provider;

    // describe('Testando o método pegar', function() {

        /*
        it('Teste pegando url brasilapi', function() {
            $classe = new MunicipioService();
            $result = $classe->pegar("sp");

            expect($result)->toBeJson();
        });
        */

        // it('Teste pegando url brasilapi', function() {
        //     $$class = new Provider();
        //     $result = $class->pegar("https://brasilapi.com.br/api/ibge/municipios/v1/SP", 1, 10, "brasilapi");

        //     getJson($result)->dump;
        //     //expect($result)->toBeJson();
        // });

        /*

        it('Teste de subtração', function() {
            $a = 10;
            $b = 20;
            $operation = 'sub';

            $result = MainOperations::mathOperation($a, $b, $operation);

            expect($result)->toBe(-10);
        });

        it('Teste de multiplicação', function() {
            $a = 10;
            $b = 20;
            $operation = 'mult';

            $result = MainOperations::mathOperation($a, $b, $operation);

            expect($result)->toBe(200);
        });

        it('Teste de divisão', function() {
            $a = 10;
            $b = 20;
            $operation = 'div';

            $result = MainOperations::mathOperation($a, $b, $operation);

            expect($result)->toBe(0.5);
        });

        */
    // });
